hosts:
  - 1513629884013:
      url: "https://wazuh.manager"
      port: 55000
      username: wazuh-wui
      password: "MyS3cr37P450r.*-"
      run_as: false
# Configuración de cookies
cookies:
  session: 24h

# Configuración de logs
logs:
  - info
  - error

# Configuración de encriptación
encryption_key: your-encryption-key

# Configuración de caché
cache:
  - enabled: true
    - time: 5

# Configuración de timeout
timeout: 120000

# Configuración de API
api:
  - request:
      - timeout: 120000
      - retries: 3

# Configuración de patrones de índices
pattern: wazuh-alerts-*

# Configuración de checks de salud
health_check:
  - enabled: true
  - interval: 30000

# Configuración de hooks
hooks:
  - before_render:
      - enabled: true